import React, { PropTypes } from 'react';

import Button1 from '../../components/button1';
import './App.css';

const App = ({ children }) => (
  <main>s
    <Button1/>
  </main>
);

export default App;
