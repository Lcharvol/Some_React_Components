import React from 'react';
import Button2 from '../button2';
import './Header.css';

const Header = props => (
  <div className="header">
    <Button2
      name="Test"
    />
  </div>

);

export default Header;
